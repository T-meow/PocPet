# 饱食溢出恢复

- 目标与授权：食物恢复超过饱食上限时，超出的恢复量按 60% 计入当前饱食；仅修改与必要检查，不提交、推送或打包。
- 决定：沿用吃撑保护与批量吃饱即停；普通喂食、冒险、远征共用计算。生日蛋糕补满且保留已有溢出，其他非食物恢复不新增溢出。保存、读档、睡眠快照与行动扣减保留剩余溢出。
- 进度：核心计算、各补给入口、存档及扣减链路已完成；恢复预览显示实际恢复与未计入部分，状态数值允许超过上限，进度条仍最多填满。
- 验证：TypeScript `--noEmit` 通过；`check-stat-scaling-batch`、`check-item-balance`、`check-adventure`、`check-expedition`、`check-offline-lifecycle`、`check-save-recovery`、`check-sleep-settlement`、`check-partner-schedule`、`check-time-pause`、`check-community` 全部通过。新增断言覆盖折算边界、小数、批量、特殊与 MOD 食物、保存/读档、睡眠、升级、行动扣减及两类旅途补给。
- 待办：用户人工验收界面显示；无待处理逻辑项。未构建安装包。
- 路径：`src/core/{petStats,itemEffects,petActions,petState,petLifecycle,petCommon,petEvents,partnerSchedule,adventure,expedition,community}.ts`，`src/ui/{ItemRecoveryPreview,ItemStorageModal,AdventurePage,StatusBar}.tsx`，相关 `scripts/check-*.ts`。
- 禁动项：现场已有 `docs/后续开发规划.md`、`scripts/check-valley.ts`、`scripts/check-community.ts`、`src/ui/{App,CommunityPage,GardenPage,HomePageV2}.tsx` 改动；不覆盖，不代提交。
