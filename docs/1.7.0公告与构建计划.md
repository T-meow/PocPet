# 1.7.0 公告与构建

- [x] 将公告拆为带小标题的短段落，突出本地备份、B 站限制与普通版 App 自动检查更新；说明下载后需自行安装。
- [x] B 站版和普通客户端在本版本前三次启动时显示，同日可重复启动。实际显示才计数，同次启动只计一次；记录独立保存在本机设置，导入、导出及恢复存档不影响记录，故障恢复仍优先。
- [x] 同步 package、Tauri、Cargo 与锁文件版本至 1.7.0，保留应用 ID、存档格式和固定 Android 签名。
- [x] 双版本构建、存档检查、公告次数以及 320/390/1280 像素宽度的布局验证通过，具备提交和触发全量构建条件。

本次授权恢复全量构建，不创建版本标签、不公开 Release、不部署 Pages。交付包沿用八种产物命名；旧测试签名 APK 的迁移仍需先导出存档后重新安装。

提交推送后使用 `gh workflow run release.yml --ref main -f full_build=true` 启动全量构建。该手动运行应包含 Windows x64/x86、Android arm64/ARMv7、Web、macOS、Linux；实际构建结果以 Actions 为准。

公告计数键为 `pocpet.edition-notice.1.7.0`，其中仅保存展示次数和最近启动标识。旧版本公告记录保留，宠物存档格式与原有恢复逻辑保持不变。普通网页版不额外弹出客户端公告。
