# 新对话开场提示词

复制下面的内容到 PocPet 项目的新作图对话，并在末尾补上本轮具体资产任务。

```text
这是 PocPet 的专门作图对话。请先读取 AGENTS.md、docs/美术规范与作图流程.md、docs/art/README.md，并核对当前 Git 状态和涉及的素材映射。

本轮先使用 mxai；GPT 模型沿用 gpt-image-2.5-sunburst，需要明确的模型对比时才加入 nano-2.0。读取当前 mxai 技能并核对真实模型列表、参数和报价。image-studio 当前接入服务有 401 认证问题，不要继续重试它来阻塞作图，也不要把 mxai 出图记成本机渠道成功。

商店已采用 C / GPT 原画风：圆润 Q 版、深褐流畅描边、奶油糖果色、左上光源、赛璐璐加轻渐变、顺滑抗锯齿。完整基准在 docs/art/prompts/shop-icons-c-original.txt。默认纯文生图；已有具体参考授权按当前记录延续，不任意扩大参考范围。优先 1K；基础小图标可用 4×4 表，第一阶段 A/I1 则按 12 道具 4×3 表和 4 厨具 2×2 表各一张。后续获准切分时输出 256×256 透明单图，场景器具保留所需母版尺寸；接入游戏时沿用既有 ID、文件名。

生成完成后先直接给我看模型原图样式，等我明确确认后再做切分、裁切、去底、修边、缩放、透明单图、深浅底/小尺寸预览等后处理；确认前只保存原图和必要请求记录，并读取尺寸/格式/哈希，不自动加工或接入。已有明确确认或替换指令则直接完成对应范围，不重复询问。Furo 四组三视图仍待选定；既往允许使用 src/assets/pet/pet_idle_sit.png 作为该角色参考，不能把商店选 C 当成 Furo 也已选 C。GPT 历史侧面仍带透视，侧背结构是待确认草稿。

生成原图、任务记录、切分中间图和 PNG 检查图放 output/imagegen/ 的新批次目录，不覆盖旧产物、不追踪测试图。不要生成 HTML 预览页，实际界面直接打开项目开发服务器检查。可复用文字保存在 docs/；不记录密钥、不修改无关素材或玩家存档、不自动提交或发布。单助手；截图、Computer Use 或视频按当前具体任务授权执行。具体数量与范围以我下面的任务为准，不因读了规范就自动开新批次。
```

截至 2026-09-14，原商店 16＋A/I1 16＋B–E 72，共 104 项核心设计已接入。A 的 4 张前景遮挡图是派生文件，不增加设计数。B–E 经用户确认后加工为 72 张运行图片，共约 2.61 MiB；来源、尺寸、哈希和备份见 docs/art/phase-b-e-adoption.json 与 docs/B-E美术原图执行记录.md。已打开真实开发页并查看厨房、料理详情、锁定奖杯；小游戏受当前 Lv.1 进度限制，仍由用户继续人工检查。期间还有花园/背包等其他文件修改，不能回退；短暂的存档版本阻塞已随这些修改解除，服务与存档现状在新会话仍需重新核对。

当前已采用的料理为 15 款非杯装自带奶白浅盘、3 款杯装保留玻璃杯；两种水果布丁、苹果派、草莓饼干千层、猪脚饭带少量暖金星芒。C 的 I2/I3 各使用自己的上一轮原图作为唯一参考重绘后获确认；新提示词在 docs/art/prompts/phase-c-plated-review/。旧裸食物提示词仅供追溯，后续调整不重复套盘、不改料理 ID。

本轮任务请另写：要画哪些资产、多少张或多少个、是只看试稿还是替换指定素材；未改变的画风与验收偏好无需重复说明。既有明确选稿及对应接入授权持续有效，不重复索要。
