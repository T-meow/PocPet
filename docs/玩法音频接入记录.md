# 新玩法音乐与音效接入

## 目标与授权

2026-09-19：用户要求为新增钓鱼、探险等玩法接入 BGM，并从 `D:\游戏素材\sfx music` 寻找音乐及音效候选。单助手；允许本地筛选、转码、源码接入和必要检查。保留现有未提交功能和上一轮标签／社区服务修改，不改版本、不提交、不发布，不操作真实存档或源素材，不使用 Computer Use／截图。

## 用户试听决定

2026-09-19 后续调整：用户已删除 `bgm_community_loop.mp3`、`bgm_fishing_loop.mp3`、`bgm_adventure_loop.mp3`，要求接入现有曲目。社区／社区服务改用 F1/F2；农场 F1/F2、钓鱼 F3/F4、冒险 A2/A5、夜间 A3/A4。下文 Friends／Flow／Journey 为历史记录，清单保留来源并标记已由用户移除；检查只核验 active 运行素材。

用户要求多找休闲轻松的候选、不能按名称直接接入。上一阶段分析 32 首完整音频，制作十首约 −20 LUFS 的完整试听 MP3；工具没有直接听辨能力，信号测量仅作初筛。2026-09-19 用户实际试听后决定八首全部保留使用：

| 场景 | 编号 | 曲目 | 运行文件 |
|---|---|---|---|
| 农场／菜园 | F1 | Afternoon Clouds Underscore LOOP | `bgm_garden_f1.mp3` |
| 农场／菜园 | F2 | Cool Easy Jazz Underscore LOOP | `bgm_garden_f2.mp3` |
| 钓鱼 | F3 | Simple Acoustic Theme #2 | `bgm_fishing_f3.mp3` |
| 钓鱼 | F4 | Simple Acoustic Theme #3 | `bgm_fishing_f4.mp3` |
| 探险／远征 | A2 | Cheerful Quirky Indie Pop Underscore LOOP | `bgm_adventure_a2.mp3` |
| 探险／远征 | A5 | Positive Orchestral Theme #4 | `bgm_adventure_a5.mp3` |
| 夜晚安静场景 | A3 | Simple Acoustic Theme #8 | `bgm_night_a3.mp3` |
| 夜晚安静场景 | A4 | Simple Acoustic Theme #1 | `bgm_night_a4.mp3` |

- 直接复制用户试听通过的 MP3，保持字节一致，不重新裁切或转码。每组依序播放两首，播完回到第一首；页面重复渲染不重播，切换页面保留该组曲目序号。完整乐曲按原长度播完，下一曲淡入，不宣称无缝拼接。
- 商店与睡眠优先；钓鱼、冒险（含前哨大厅）／远征保持各自主题。其余页面沿用游戏本地时间 22:00–07:00 播放夜晚组；白天社区农场、菜园、果园与社区服务用 F1/F2，首页沿用小窝曲。农场地点窗口共用同一播放组，开关窗口不切曲；关闭商店后恢复对应场景和曲目序号。
- 沿用总开关、手势解锁、页面隐藏静音和淡入。切曲后已离开的音频结束事件不能触发重播；静音时不启动下一曲，恢复同一场景继续当前进度。
- 初期选取的 Friends／Flow／Journey 三个运行 loop 文件已由用户删除，素材清单标记 `removed-by-user`，不再注册或校验这三个运行文件；来源与历史测量仍可追溯。F5／A1 的源素材与试听文件保留。

补充少量短音效：小水花（抛竿）、轻水花（提竿／收线）、清脆提示（咬钩）、摘取植物（种养收获）、泥土脚步（探索推进）。成功／发现／返程与建设优先复用现有提示音。只在实际状态推进后触发，避免失败点击报喜、刷新重播或计时器重复鸣响。

## 进度与验证

- 已完成正式 App 接入：community／garden／fishing／adventure／night 场景音乐；公共点击事件解锁音频，状态提交后的 effect 播放实际成功的操作反馈，避免 React 重复求值重播。社区页签状态由正式 App 持有，独立原型仍支持组件内部状态。
- 已接入钓鱼抛竿／咬钩／提竿收线／松线／成功／脱钩／领取；社区种养／修复建设／经营和领取；冒险与远征出发／推进／发现／休整／返程及领取。
- 咬钩按 `biteAt` 单次定时，每竿去重；过期或已提竿不播，离开社区与卸载清除计时器。后台及总开关静音仍由音频引擎控制。初次渲染、切换角色／页面、重复状态与失败操作不播放成功反馈。
- 本阶段新增八首已选 BGM，共 **10,321,431 字节（9.84 MiB）**，位于 `src/assets/audio/bgm/`。MP3／44.1 kHz／立体声／128 kbps，保留试听阶段约 −20 LUFS 的静态增益；八首原素材、试听文件、运行副本的大小／SHA-256 校验及运行副本完整解码均通过。
- 原社区 Friends 为约 −18 LUFS；五个短音效为单声道／96 kbps。来源原路径、源／输出大小、完整 SHA-256、增益、编码和时长见 [素材清单](玩法音频素材清单.json)。源素材只读。
- `npx.cmd --no-install tsc --noEmit` 通过。
- `npx.cmd --no-install tsx scripts/check-world-audio.ts` 通过：昼夜边界、场景优先级、全部八首实际轮播／回绕、商店返回曲序、过期音频事件、解锁、后台和开关静音／恢复、钓鱼完整链、咬钩去重／过期、失败操作、种植收获、远征故事、素材哈希。
- `npx.cmd --no-install tsx scripts/check-ui-v2.ts` 通过：普通／Toy、双语言 React 渲染和现有 CSS 约束；`git diff --check` 通过。
- 曲风及用途已由用户试听确认。实际游戏切曲衔接、BGM 与咬钩／操作音效相对音量仍留给用户人工体验；没有运行浏览器或自行听感验收，未打包或发布。

## 试听与输出

- [历史试听页](../output/audio-candidates/20260919/index.html)、[候选清单](../output/audio-candidates/20260919/README.md)、[原路径／SHA-256／测量](../output/audio-candidates/20260919/manifest.json)、[32 首全量分析](../output/audio-candidates/20260919/measurements.json)。十首试听 MP3 合计 13,090,498 字节（12.48 MiB）；页面 F／A 原分组仅供追溯，正式用途以本文的用户决定为准。
- [用户已选八首及运行文件校验](../output/audio-candidates/20260919/approved-selection.json)。

## 路径

- 音频注册：`src/core/audio.ts`。
- 集成入口：`src/ui/App.tsx`、`src/ui/CommunityPage.tsx`；场景与结果判定在 `src/ui/app/worldAudio.ts`，状态监听／咬钩计时在 `src/ui/app/useWorldAudioFeedback.ts`。
- 原素材只读；新运行素材放在 `src/assets/audio/`。
