# 1.9 冒险新增图标素材需求

更新：2026-09-18。范围：本次冒险已实现的补给、工具与战利品，合计 **6 个独立图标**。原图已获用户确认并接入游戏；正式运行图为 128×128 透明 WebP，PNG 母版与原图保留。

## 本批作图清单

| 顺序 | 名称／道具 ID | 造型与识别重点 | 游戏用途 | 正式运行文件名 |
| --- | --- | --- | --- | --- |
| 1 | 便携坚果包 `trail_mix` | 打开的米色便携小袋，露出少量杏仁、核桃与果干；暖棕色食物配薄荷绿封口。靠包装区别于厨房已有的散装混合坚果。 | 途中补给；基础饱食 +24、体力 +12 | `item_trail_mix.webp` |
| 2 | 野果诱饵包 `berry_bait` | 小布袋装红紫色野果，果实略露出，细绳收口、一片绿叶；轮廓和色块与坚果包明显不同。 | 引诱动物，一次消耗一份；不能喂食 | `item_berry_bait.webp` |
| 3 | 探路绳 `trail_rope` | 暖米色粗绳盘成一束，用短绑带束住，露出一小截绳头；适量绞纹即可，避免密集细线。 | 独立工具位，可重复使用 | `item_trail_rope.webp` |
| 4 | 一堆金币 `coin_hoard` | 两三摞高低不同的圆金币，前方少量散币，紧凑成一堆；暖金色、清晰厚度，与顶部单枚货币图标区别。 | 战利品，每份兑换 **360 金币** | `item_coin_hoard.webp` |
| 5 | 溪谷琥珀 `valley_amber` | 一块溪水磨圆的不规则蜂蜜色琥珀，内部封存一片小叶；少量通透层次，轮廓圆润，避免画成多面钻石。 | 战利品，每份兑换 **600 金币** | `item_valley_amber.webp` |
| 6 | 古老金条 `ancient_gold_bar` | 一根略厚的短金条，梯形块体、磨圆旧边，少量浅刻痕与深金色旧痕；无现代品牌、数字或可读铭文。 | 战利品，每份兑换 **1000 金币** | `item_ancient_gold_bar.webp` |

每个战利品占一份行囊容量。溪谷入口首通与每日重探均从三种战利品中等权随机获得 **1 件**，另给 22 基础小心心并沿用成长／增益；物品随行程固定，重载不重抽。旧规则 1／2／3 在途行程保留原奖励。

## 画风与交付

- 沿用已采用的商店 C 组：圆润的 2D 卡通插画、深巧克力色顺滑描边、柔和上色、适量材质细节、左上光源、轻微俯视三分之四视角。与现有料理及物品图的体量保持一致，48×48 时仍能辨认。
- 后续制作建议一张 **3 列 ×2 行、共 6 项**原图表，按上表从左到右、从上到下排列，3:2 画幅、纯白背景、格间充分留白。物体完整，不画网格、标签、角色、文字、水印或地面投影。
- 按项目现行流程采用 mxai 的 `gpt-image-2.5-sunburst`，默认 `quality: medium`、`resolution: 1K`；正式作图前核对当时可用模型与报价。沿用商店纯文生图，后续如使用参考图需按当轮授权。
- 先展示模型原图，待用户确认样式再加工。确认后导出 **256×256 RGBA 透明母版**及 **128×128 RGBA 游戏用图**，主体最长边分别约 184／92 px，居中留边，使用 Lanczos3 缩放。记录原图实际尺寸、来源、哈希与加工结果。
- 正式运行文件放入 `src/assets/icon/`，在 `src/assets.ts` 只替换资源映射，保留上述道具 ID。各处背包、整备、拾取、结算及商店共用一张物品图，不重复计设计数量。

制作依据：[美术规范与作图流程](美术规范与作图流程.md)。本批已完成切分与接入，最终运行格式与体积见 [素材压缩记录](美术素材无损压缩记录.md)。

## 可以复用的素材

| 场景／物品 | 本轮处理 |
| --- | --- |
| 苹果、橙子、胡萝卜蛋饭、蛋炒饭、能量饮料、金苹果 | 已有正式物品图，继续复用 |
| 出发整备、背包、手账、地图、运输、状态条等功能按钮 | 继续使用现有 Lucide 矢量图标，不新增位图 |
| 首页冒险入口 | 裁切已接入的大厅／溪谷背景，不另画图标 |
| Furo、Doro、mint | 复用既有角色与开心状态图；摇晃／迈步为界面动画 |

## 另列的背景需求

五地区总地图仍需要独立底图：溪谷、风车山丘、雾松林地、潮汐海岸、旧观测站，共 **5 张**，属于后续背景批次，不计入本批 6 个图标。现有大厅与溪谷入口背景已接入；其余节点场景按任务开放顺序另行制作。

总地图底图按 1000:650 比例，预留交互地标位置；文字、路线、节点按钮和伙伴不烘焙进图片。配置与坐标见 [实现计划的地图素材记录](冒险与前哨基地实现计划.md#后续地图素材接入)。

## 接入位置与进度

- 道具定义／兑换值：`src/core/adventureItems.ts`。
- 原 SVG 占位图留档，但共用物品映射已改用 `src/assets/icon/item_*.webp` 中的六张正式图。
- 图标映射：`src/assets.ts`；复用入口 `resolveItemIcons`，Mod 覆盖继续有效。
- 当前状态：6 项已接入整备、背包、商店、拾取及结算使用的共用映射；最终界面观感由用户人工验收。

## 2026-09-17 · 首批图标原图制作

- 目标／授权：用户要求使用作图技能制作图标，按本清单生成六项的一张原图表。使用 mxai 技能，单助手，画面由用户检查。
- 决定：Sunburst、medium、1K、3:2、1 张、三列两行，纯文生图。先核对当前模型与报价，正式参数与 dry_run 一致；原图确认后再加工，不自动接入游戏。
- 路径：清单 [adventure-icons-20260917.json](art/adventure-icons-20260917.json)、[完整提示词](art/prompts/adventure-icons-20260917.txt)；任务记录与原图在 `output/imagegen/adventure-icons-20260917/`。
- 完成：任务 `2100604126357360640` 经真实查询到达 `status: 2`。原始 PNG 已保存为 [adventure-icons-raw.png](../output/imagegen/adventure-icons-20260917/adventure-icons-raw.png)，实际 **1536×1024**，**1,654,724 字节**；PNG 结构与压缩数据读取通过，未做画面验收。正式请求与 dry_run 一致，报价 3 积分；报价不冒充另行核对的账户实扣。没有重试或追加生成。
- 原图 SHA-256：`7411c474247b816e965446fa77a3cd5c083036f662fbd3048775b477b998f456`。完整提示词、请求／提示词哈希、提交响应、轮询终态、原始 URL 与只读验证结果保存在本批目录。
- 工具版本：本机 mxai 1.4.12；服务返回最新版 1.5.2，本次已有接口校验正常，没有修改技能安装。
- 该生成阶段结束时只交付原图，未加工或接入；后续用户已确认并授权接入，见下节。

## 2026-09-18 · 接入完成

- 按实际空白走廊切分，移除边缘连通白底，保留内部高光；256×256 透明母版与 128×128 PNG 源图均留档。运行文件采用体积更小的 WebP，六张共 **74,742 字节**，道具 ID 与 Mod 覆盖入口保持兼容。
- [接入清单](art/adventure-icons-adoption-20260917.json) 记录原样切块、母版、最终运行路径与哈希；[最终验证](art/asset-compression-verification-20260918.json) 记录尺寸、引用和实际开发服务响应。
- 类型检查、冒险流程、主界面回归及图片导出检查通过。预览使用现有 `http://127.0.0.1:5174/`，未改玩家存档；图标边缘与界面观感交用户检查。
