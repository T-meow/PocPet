# 美术文件清理记录

## 2026-09-17

- 目标／授权：用户要求清理 `docs/art/` 及其他生图相关位置的无用文件，再提交全部本地改动。单助手，只检查文件与引用，不生成图片、不做画面检查。
- 决定：清理 10 个仓库文件（5 份一次性脚本副本、3 份已合入完整提示词的片段、1 份重复 Nano 提示词、1 份旧渠道诊断）；清理 22 个忽略文件（5 个旧 HTML 预览页、4 张已被修订版替代的加工预览、13 张与修订版逐字节相同的源切块）。合计 32 个文件、5,299,350 字节。
- 保留：全部生成原图、请求／真实任务与验证记录、现役 `src/assets/`、母版、最终预览、对照 PNG／ZIP、视频及其源素材。未确认的 Furo／冒险图标原稿继续保留；不按“暂未接入”判定无用。
- 提交前留档：基线 `aa51d17c1a795405fc9efc5ab18230eb1dee27e4`；工作区已有冒险、农场、版本和原型删除等改动。本地 ZIP 已保存其中现存文件、未跟踪文件及清理候选，共 115 个文件，既有删除路径另记清单。
- 备份：`output/cleanup/art-2026-09-17T15-34-32-959Z/before-cleanup.zip`，6,902,414 字节，SHA-256 `9eabb1297c61f47cfeb558d65acca34780d6c2eb0f27bef88655777147d39aee`；已解压读回并逐项核对哈希。精确删除路径、理由、替代文件及 1,513 个受保护文件的基线哈希见同目录 `plan.json`。
- 恢复：ZIP 按项目相对路径保留文件；恢复时只取需要的条目，避免覆盖后续改动。已跟踪的旧文档也可从上述 Git 基线读取。
- 进度／验证：32 个候选已逐项校验后删除，1,513 个受保护文件哈希未变。重复提示词和 13 张切块哈希相同，三份提示词片段完整存在于保留的全文；5 份旧脚本与仍保留的原脚本哈希一致。7 份调整文档的 162 个本地链接、22 份美术 JSON、32 处提示词引用／记录的哈希、5 份原始附件和 `git diff --check` 均通过。备份中的 64 个代码／正式素材文件未被本轮清理改动。
- 提交验证：此前 TypeScript、冒险、厨房／小游戏、物品数值、社区日程、存档恢复、1.9.0 版本一致性、双版本双语界面及农场原型检查均通过；清理未改业务代码，不重复运行。
- 待办：无清理待办，随全部本地改动一并提交；美术选稿与游戏画面仍按各自任务记录由用户人工验收。
- 禁动项：玩家存档、账号、原图、原始任务记录、已生成 APK、`release/` 和本次清理范围外文件；不推送或发布。
