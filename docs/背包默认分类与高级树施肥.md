# 背包默认分类与高级树施肥

- 目标/授权：背包每次打开默认食物；仅高级树（摇钱树、金苹果树）支持多份肥料，普通树保留每轮一次的规则。用户不需要画面检查；不使用 Computer Use，不提交或发布。
- 决定：高级树普通肥每份减本轮自然时长 1%，爱心肥 2%；每块地两种肥合计每日最多 10% 且不超过 6 小时，沿用游戏每日重置和本轮养护 60% 上限。达到上限不继续扣物品；高级树肥料只减时，营养剂产量加成保持原规则。
- 进度/验证：已完成背包默认食物、高级树数量选择/实际扣除份数/每日额度提示、每日施肥记账。每日 05:00 重置；收获、清理、重种和存档重载保留当日已用额度。花园 schema 升至 6，兼容旧存档，空地压缩保存保留未清零额度。高级树肥料仅减时；普通树与营养剂原规则保留。
- 检查：TypeScript、check:garden-care（多份等价、两种肥混用、两种高级树、库存不足、每日百分比/6 小时上限、05:00 重置、每轮上限、最低剩余时间、旧存档、存档往返和重种）、check:item-balance、check:time-guard、check:save-recovery、check:gacha 均通过；git diff --check 通过。http://127.0.0.1:5173/src/ui/GardenPage.tsx 返回 HTTP 200，未做画面检查。
- 待办：本次功能完成，用户试用。额外运行 check-save-v2.ts 时，存档相关段落通过，随后第 250 行饼干箱旧价格断言失败（期望 500、当前 280）；未改动此次任务以外的价格测试。
- 路径/禁动项：主要涉及 src/core/garden.ts、petTypes.ts、src/ui/GardenPage.tsx、App.tsx、useGardenController.ts、i18n、scripts/check-garden-care.ts、check-item-balance.ts；工作区已有美术素材、厨房/小游戏、物品卡片与相关文档改动全部保留。

## 花园管理弹窗与树上产物（2026-09-14）

- 授权：管理操作改为弹窗，可收获道具呈现在树上，表现为结果实；用户明确不需要人工视觉检查，自行试用。
- 完成：原展开列表改为管理按钮与 DialogShell 弹窗，显示栏位、树种、剩余时间以及原有施肥/营养剂/移除操作；高级树份数和额度继续实时更新。移除先关闭管理弹窗，再走原确认流程；成熟时可在弹窗或树下收获。
- 完成：道具与金币按种类分散挂在树冠上，带数量角标、完整名称/数量提示；收获后按树状态移除挂果显示。仅调整 GardenPage.tsx 与 ui-v2.css 的花园部分，保留此前与其他任务改动。
- 验证：TypeScript 与本次文件 git diff --check 通过；测试服务器已重新启动，GardenPage.tsx 与 ui-v2.css 返回 HTTP 200。未截图、未做画面检查，待用户在 http://127.0.0.1:5173/ 试用。
