# 背包默认分类与高级树施肥

- 目标/授权：背包每次打开默认食物；仅高级树（摇钱树、金苹果树）支持多份肥料，普通树保留每轮一次的规则。用户不需要画面检查；不使用 Computer Use，不提交或发布。
- 决定：高级树普通肥每份减本轮自然时长 1%，爱心肥 2%；每块地两种肥合计每日最多 10% 且不超过 6 小时，沿用游戏每日重置和本轮养护 60% 上限。达到上限不继续扣物品；高级树肥料只减时，营养剂产量加成保持原规则。
- 进度/验证：已完成背包默认食物、高级树数量选择/实际扣除份数/每日额度提示、每日施肥记账。每日 05:00 重置；收获、清理、重种和存档重载保留当日已用额度。花园 schema 升至 6，兼容旧存档，空地压缩保存保留未清零额度。高级树肥料仅减时；普通树与营养剂原规则保留。
- 检查：TypeScript、check:garden-care（多份等价、两种肥混用、两种高级树、库存不足、每日百分比/6 小时上限、05:00 重置、每轮上限、最低剩余时间、旧存档、存档往返和重种）、check:item-balance、check:time-guard、check:save-recovery、check:gacha 均通过；git diff --check 通过。http://127.0.0.1:5173/src/ui/GardenPage.tsx 返回 HTTP 200，未做画面检查。
- 待办：本次功能完成，用户试用。额外运行 check-save-v2.ts 时，存档相关段落通过，随后第 250 行饼干箱旧价格断言失败（期望 500、当前 280）；未改动此次任务以外的价格测试。
- 路径/禁动项：主要涉及 src/core/garden.ts、petTypes.ts、src/ui/GardenPage.tsx、App.tsx、useGardenController.ts、i18n、scripts/check-garden-care.ts、check-item-balance.ts；工作区已有美术素材、厨房/小游戏、物品卡片与相关文档改动全部保留。

## 花园管理弹窗与树上产物（2026-09-14）

- 授权：管理操作改为弹窗，可收获道具呈现在树上，表现为结果实；用户明确不需要人工视觉检查，自行试用。
- 完成：原展开列表改为管理按钮与 DialogShell 弹窗，显示栏位、树种、剩余时间以及原有施肥/营养剂/移除操作；高级树份数和额度继续实时更新。移除先关闭管理弹窗，再走原确认流程；成熟时可在弹窗或树下收获。
- 完成：道具与金币按种类分散挂在树冠上，带数量角标、完整名称/数量提示；收获后按树状态移除挂果显示。仅调整 GardenPage.tsx 与 ui-v2.css 的花园部分，保留此前与其他任务改动。
- 验证：TypeScript 与本次文件 git diff --check 通过；测试服务器已重新启动，GardenPage.tsx 与 ui-v2.css 返回 HTTP 200。未截图、未做画面检查，待用户在 http://127.0.0.1:5173/ 试用。

## B 站移动端背包滚动与卡片高度（2026-09-15）

- 目标/授权：用户要求修复无法滑动、喂不了其他食物的问题，并允许效果标签覆盖部分图片以缩短卡片；随后明确要求提交 B 站 Toy 审核。沿用本次只做代码检查的决定，单助手，不使用 Computer Use；本次仅授权 Toy 更新与审核，不做 Git 提交、GitHub 推送或其他平台发布。
- 决定：目录和详情共享受 fixed 遮罩实际可用高度约束的布局，取消背包对 dvh 的依赖；目录只由物品列表纵向滚动，详情只由详情内容滚动。开关弹窗恢复焦点时不带动滚动；效果标签绝对定位覆盖图片，手机三列、超窄屏两列。
- 进度/验证：已完成样式与焦点恢复修改；TypeScript、现有 check-ui-v2.ts（普通版/Toy 版双语 React 渲染、五组尺寸 CSS 检查）通过。已读回修改片段，复核层级、手机/桌面尺寸覆盖、详情内滚动与标签点击穿透；git diff --check 通过。没有进行截图或真机验证。
- Toy 提交：沿用 package.json 的 1.8.5，从 e1845a5205544a12ff4a64f78dacf73053292ba5 加本次未提交修复构建 dist-toy（199 个文件、22,790,073 字节）。build:toy、check:release -- --dist dist-toy --edition bilibili、check:toy 与 toy_doctor.py 均通过，内容预检无 ERROR/WARN。
- 上传核验：预览 preview_eskOhSUq 的入口、JS、CSS 和 build-info.json 均 HTTP 200，后三者 SHA-256 与本地一致。入口 assets/index-Bv52ldUB.js：15418bd085c8d98c21567534f87ec017d9af4c9f1fcfd3145d3508491f58f386；assets/index-fkcxOfA0.css：4b368aca77b659ebd18dcbfc663747bc25c9ad4c007b02665b4588efe49cffc4。
- 审核结果：按用户明确授权执行 toy update 23949807352832 D:/Projects/PocPet/dist-toy --json --yes，返回 auditing；随后 mylist 单次读回确认 PocPet（23949807352832）仍为 auditing。提交预览：https://www.bilibili.com/toy/preview/preview_FnZYpPTk/index.html；正式地址：https://www.bilibili.com/toy/pocpet/index.html。
- 待办：审核已提交；等待平台审核，B 站实际触摸体验由用户后续验收。未做 Git 提交、推送、其他平台发布或真机检查。
- 路径/禁动项：src/styles/item-storage.css、src/styles/ui-v2.css、src/ui/ItemStorageModal.tsx、src/ui/DialogShell.tsx、scripts/check-ui-v2.ts 与本记录。保留工作区现有 AGENTS.md、美术及视频文档和未跟踪美术任务文件。

## 厨房与小游戏滚动补充修复（2026-09-15）

- 目标/授权：用户反馈做饭和小游戏仍有同类无法滑动问题，排查后明确要求修改。沿用代码检查、用户自行视觉验收；单助手，不使用 Computer Use，不提交或发布本次修复。
- 决定：厨房主界面、食谱详情、做饭过程/出炉结果、小游戏列表/游戏中、小游戏结算共五处 DialogShell 接入 activity-backdrop。使用 fixed 遮罩实际可用高度及百分比限高，覆盖旧 dvh 规则；外层不滚动，内部内容纵向滚动，结算页由弹窗本身滚动。抛球、吹泡泡专用手势保持原样。
- 进度/验证：布局与滚动修改完成并 UTF-8 读回；TypeScript、check:companion-activities（做饭过程/结果、三种小游戏、手势与暂停恢复、界面渲染）和 check-ui-v2.ts（普通版/Toy 版双语）均通过，相关 git diff --check 通过。复核五处弹窗接入、CSS 覆盖优先级、单一纵向滚动区域及游戏手势；没有进行截图或真机验证。
- 开发服务：http://127.0.0.1:5173/ 继续运行，四个修改组件和合并样式均 HTTP 200，已确认提供本次新内容。
- 待办：用户刷新开发页面，检查做饭及小游戏的实际滑动体验；本次未提交或发布。
- 路径/禁动项：src/ui/KitchenModal.tsx、src/ui/PlayModal.tsx、src/ui/kitchen/KitchenCookingModal.tsx、src/ui/play/MiniGameResultModal.tsx、src/styles/companion-activities.css、src/styles/kitchen-cooking.css、src/styles/ui-v2.css。本轮发现其他任务新增节日食物源码、美术及检查脚本，全部保留，不改动其内容；此前背包和致谢名单改动继续保留。
