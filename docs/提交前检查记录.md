# 提交前检查记录

## 目标／授权

- 2026-09-14：用户要求检查当前所有未提交改动、排查并修复 bug，然后完成本地 Git 提交；范围包含现有修改与未跟踪源码、正式美术和流程文档。
- 单助手；沿用用户自行人工视觉验收的决定，不使用 Computer Use、截图或生图。无需重复确认提交，不推送、发布或打包。

## 范围／决定

- 起始 225 个文件：源码／样式／本地化 50、检查脚本 11、正式图片 108、说明文档 11、美术流程附件 45；约 6.6 MB。
- 全部保留现有工作，先复查差异与未跟踪内容，再完成必要测试和最小修复，最后统一提交。真实玩家存档、账号、输出目录和发布产物不操作。
- 重点：社区服务 schema 7 的渐进扣耗／提前结束／跨日与迁移，花园 schema 6 的每日施肥额度，扭蛋和金苹果，新增 UI 与资源接入。

## 进度／验证

- 已核对 Git 状态、变更清单、项目规则和各功能执行记录。
- 已复查社区服务、花园、扭蛋／金苹果、背包、厨房／小游戏／奖杯的核心及界面接入。103 个 PNG 通过 CRC／压缩数据校验，5 个 WebP 结构完整，133 处资源引用存在且大小写匹配；UTF-8、JSON、1701 个双语键和参数一致，13 份历史附件的字节数／SHA-256 与归档记录一致。
- 检查期间工作区新增社区改版十张扭蛋券礼包及浮动入口，纳入本次提交；已复查旧档资格、新档排除、整份容量检查、领取回执与弹窗去重。
- 已通过：社区服务、花园、离线、时间回拨、存档 v2、模拟云存档 v2、存档恢复、厨房／小游戏、成就和睡眠结算检查。复用了此前通过且逻辑未再修改的扭蛋、道具平衡和批量数值检查；礼包相关改动完成后，TypeScript 及普通版／Toy 版双语 UI 检查再次通过。
- 发现并修复：`check-sleep-settlement.ts` 仍按金苹果旧恢复 30 点断言；改为 Lv.1 恢复 50 点后的心情 90、体力 70，保留睡眠结算和唤醒惩罚顺序的回归覆盖。
- 本轮技术检查完成，未发现已复现的运行逻辑缺陷；修正了两项旧测试断言。人工视觉验收由用户负责，自动检查不替代画面验收。
- 最终范围为 232 个文件（含本记录、睡眠断言修正和检查期间新增的社区礼包）。受检文件以 SHA-256 快照比对，暂存后再次核对差异和工作区；本记录随所有受检修改统一本地提交。

## 路径／禁动项

- 主要路径：`src/core/`、`src/ui/`、`src/styles/`、`src/assets/`、`src/*Assets.ts`、`src/i18n/`、`scripts/check-*.ts`、`docs/`。
- 不覆盖或删除现有工作；美术归档中的历史脚本文本仅审阅，不执行。未授权推送、版本标签或发布。
