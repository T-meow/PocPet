# 商店图标美术试稿

后续制作以 [美术规范与作图流程](美术规范与作图流程.md) 为入口：当前使用 mxai，商店已接入 C 组。本文保留各轮发生时的要求和观察；第三轮的「未接入」描述已由后续 C 组接入记录更新。

## 目标与授权
- 最新指令（2026-09-13）：用户选择视频中的 C 组（GPT 原画风），要求替换当前图标并打开开发服务器人工查看。授权接入对应 16 张源图标，沿用用户人工视觉验收偏好。
- 第三轮当前指令：读取几个角色的画风，用相同 16 个商店道具尝试另一种画风对比。沿用 mxai GPT/Nano 各一张、纯文生图、不上传原图、切分交付；本轮生成已获授权。
- 当前有效指令：用户先明确授权使用 mxai 同名 GPT 模型，随后要求重写提示词、两张都重新生成，只提取当前画风，不输入任何原图。第二轮为纯文生图，GPT 与 Nano 各一张；原第一轮素材保留。
- 2026-09-13：用户要求按现有像素风，用本机 GPT Image 与 mxai Nano 2 各生成一张 1K 多图标表格，切分后比较。
- 本轮生成各一张，共两张；选 16 个已有商店道具，4×4 排列。人工视觉检查偏好已询问；用户原始要求为先看对比。
- 单助手；此前仅生成试稿，本次按最新指令接入 C 组图标；不提交、不发布。

## 决定
- 第三轮参考观察（仅本地查看、不上传）：默认 pet_idle_sit、Doro content、Mint content。共同特征为 Q 版圆润轮廓、深色流畅描边、柔和渐变/赛璐璐明暗、少量亮点；改为顺滑卡通插画，无像素阶梯。
- 第三轮输出：output/imagegen/shop-icons-20260913-character-style/；同样 4×4，物件和格位保持与第二轮一致，额外制作前后风格对照图。
- 第三轮人工视觉检查：用户明确选择“需要，我看对比后决定”。GPT 任务 2098958829487132672、Nano 任务 2098958831869497344 已提交，均无图片输入。
- 第二轮：mxai 的 gpt-image-2.5-sunburst 与 nano-2.0；相同文字提示词，1:1、1K，GPT high/1 张。请求完全不含 input_images 或参考图片字段。
- 第二轮输出：output/imagegen/shop-icons-20260913-textonly/；第一轮只作历史留档。
- GPT：image-studio 的 gpt-image-2.5-sunburst，Images 编辑接口，high，1024×1024。
- Nano：mxai 的 nano-2.0，1:1，1K；真实模型列表已确认。
- 两者使用同一张现有素材参考表及同一份提示词。原始图片保留，切分结果独立存放。
- 顺序：苹果、香蕉、橘子、西瓜；苏打饼干、便当、草莓蛋糕、草莓牛奶；玩具球、绘本、小花束、缎带铃铛；沐浴露、湿巾、药品、小毯子。
- 输出：output/imagegen/shop-icons-20260913/。

## C 组接入（2026-09-13）
- 基线提交 ed49129，开工时 Git 工作区干净；已核对 src/assets.ts 和试稿 items.json 的 16 个对应文件。
- 来源：output/imagegen/shop-icons-20260913-character-style/gpt/icons/。保持原文件名及 128×128 尺寸，用 Lanczos3 将 256×256 透明试稿缩小。
- 共用图标的商店、背包、奖励等界面同步生效；birthday_cake 与 strawberry_cake 共用 icon_berry_cake.png。
- 已替换 16 张 128×128 RGBA PNG，共 262,567 字节；输入逐文件 SHA-256 与原 GPT 试稿 manifest 一致，输出均有透明背景，最小透明边距 15 px，写回哈希一致。
- 校验记录：output/imagegen/shop-icons-20260913-character-style/adoption-c-verification.json；保留源试稿及基线哈希，可从 ed49129 恢复原素材。
- 开发服务器已在后台运行（node scripts/serve-dev.mjs --port 5173），预览 http://localhost:5173/；首页与素材模块返回 200，16 张 HTTP PNG 均与新文件哈希一致，git diff --check 通过。
- 本轮不截图；用户打开商店、背包人工查看实际效果，再决定后续调整。更改范围为 16 张图标和本记录，未提交或发布。

## 第三轮完成与验证
- GPT 任务 2098958829487132672、Nano 任务 2098958831869497344 均已轮询至 status=2，未上传任何参考图片；两者请求提示词完全相同。
- GPT 原始 1280×1280 PNG，1,639,856 字节，SHA-256：9cede65d4ad8da92cc5dbd71a4bed9a6d48d65466ad3332ba42f1e7af34e0169。
- Nano 原始 1024×1024 PNG，1,396,586 字节，SHA-256：a491ee5ca20f6a72b9da6c2bf844d93bbe54d63e6f16f8d0a09a734bbeb353e9。
- 已按实际空白切分，改用 Lanczos3 平滑缩放并统一主体最长边 184 px；两张交付表均为 1024×1024，每套 16 个 256×256 透明 PNG，最小边距 36 px，切线没有主体像素。
- 已核验真实尺寸、透明通道、格位、边界、PNG 与 SHA-256；本轮比图及深色预览已查看。Nano 更简洁柔和，贴近角色的卡通上色；GPT 多了高光与细节纹理。透明试稿仍有少量细白边，未作为正式素材接入。
- comparison.png 对比本轮 GPT/Nano；style-comparison.png 上方是第二轮像素版、下方是本轮角色画风，同列保持同模型；compare.html 可切换尺寸和底色。
- 素材包 shop-icons-character-style.zip：14,191,631 字节，SHA-256：c7a0db8f920bc8afba1c30d8fc87ec3e7de9ae9cd2e86ecd2a02d79284d7f681；ZIP 条目已核验，透明 PNG 各 16 张。

## 第二轮完成与验证
- 两次请求已验证：纯文字、无图片字段、提示词完全相同。模型为 mxai 的 gpt-image-2.5-sunburst（high/1 张）与 nano-2.0，均请求 1:1、1K。
- GPT 任务 2098952101349560320；Nano 任务 2098952103698370560，均已轮询至 status=2。
- 原图完整保留：GPT 实际 1280×1280 PNG、1,471,646 字节；Nano 实际 1024×1024 PNG、1,334,051 字节。
- GPT 原图 SHA-256：1cc78b08cfa1e4a7ce47e6e5f367eab4ba393f8647b30484652152a5c0c1e000；Nano：8fc124cc03296758ad800dd3094956b767e7adfdb9f2c0cedeef2776da5f6149。
- GPT 第四行顶部偏高，按等分线切会碰到瓶盖。已按实际空白走廊裁切，保留无损 source-crops，再以最近邻统一主体最长边为 184 px、居中排回 4×4 的 1024×1024 交付表。
- 两组共 32 张透明 256×256 PNG，以及对应白底单元格；主体边距至少 36 px。尺寸、透明通道、裁切边界和逐图 SHA-256 已核对，详见第二轮 verification.json。
- 画面观察：Nano 色块更简洁、描边更粗，接近现有主要图标；GPT 高光、纹理和材质更丰富。两套铃铛均保留蝴蝶结；深色底仍有少量细白边，当前仅为选风格试稿。
- 交付：output/imagegen/shop-icons-20260913-textonly/；comparison.png 为 GPT/Nano 两列，compare.html 可切换尺寸与底色。
- 素材包 shop-icons-textonly.zip：12,374,732 字节；SHA-256 为 2d0990769d1c6809cddc74c8879b6e27e7229227224ca397969e7ae3162aa967；包内两组透明 PNG 各 16 张，已检查 ZIP 条目。

## 第一轮历史与现场
- 已检查 Git status 和相关 diff；工作区已有大量未提交改动，本任务保留这些内容。
- 已读取商店定义与图标映射；现有抽查图标为 128×128 RGBA，物件占比不同。
- 已检查模型配置存在；不记录密钥。沿用已安装 Node、Python 和 Codex 自带 sharp，不安装依赖。
- GPT 原渠道调用被拒绝：HTTP 401: Invalid API key；gpt-raw.job.json 状态 rejected，没有生成 GPT 图片。用户后续已授权 mxai，完成结果见第二轮。
- mxai 对 GPT 同名模型的无生成报价校验通过：1K、high、1 张，7 积分；该报价请求未带参考图，也未创建任务。
- Nano 2.0 任务 2098948944838004736 已轮询至 status=2，原图实际 1024×1024 PNG，1,043,238 字节。
- Nano 原图 SHA-256：80e735a4a3afd73203c955118a8334befd168c74cd3dd6f4a44b791bcedfeac0。
- 已导出 16 张 256×256 原格、16 张 256×256 透明图及透明总表；所有切分边缘无主体像素，最小留白 35 px；逐图尺寸、透明通道、SHA-256 见 nano/manifest.json。
- 画面复核：Nano 很大程度沿用参考，蛋糕与花束依然较细腻；铃铛漏画粉色缎带。透明图在深色底上仍有细白边，当前作为比较试稿，尚未达到正式替换素材的边缘质量。
- compare.html 可切换显示尺寸及背景；comparison.png 当前仅比较原图与 Nano，未把原图冒充 GPT 结果。

## 待办
- 本次按用户选择接入 C 组；用户在开发服务器查看实际效果后决定进一步调整。

## 路径与禁动项
- 参考源：src/assets/icon/；映射：src/assets.ts；道具：src/core/items.ts。
- 本次仅替换 items.json 对应的 16 张 src/assets/icon/ PNG；业务代码、其他源素材与已有生成产物保持不动。
