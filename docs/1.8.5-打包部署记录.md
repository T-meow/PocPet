# PocPet 1.8.5 打包与部署记录

## 目标／授权

- 2026-09-14：用户要求本地 Windows EXE、Android APK 测试包，推送 GitHub，部署 GitHub Pages 与 B 站 Toy，并明确版本提升到 1.8.5。
- 单助手；人工视觉检查由用户负责。按本次明确部署指令完成现有站点更新，不重复请求发布确认。
- 本地范围为 Windows x64 与 Android arm64，APK 沿用既有 debug keystore；同名产物先备份。构建所需网页目录仅用于原生嵌入与 Toy 上传。

## 决定／路径

- 源码起点：`27a7876`；同步 package、Tauri、Cargo 与本地版本回退字段为 1.8.5。
- GitHub：`T-meow/PocPet`，将经过检查的提交推送至 `main`；Pages 使用 `pages.yml`、`source=main` 和明确的版本／提交。
- Toy：更新现有 PocPet，ID `23949807352832`，地址 `https://www.bilibili.com/toy/pocpet/index.html`；先检查构建内容和生成预览，再按用户本次部署授权提交审核。
- 产物：`release/pocket1.8.5.exe`、`release/pocket1.8.5.apk`。不新增正式 Release 标签，不生成额外架构；安装包、构建缓存、账号与真实存档不提交。

## 进度／验证

- 开始时工作区干净，远端主分支可快进，Toy 目标和 Pages 配置正确；没有同名 1.8.5 旧包需要备份。
- 版本提交：`b4cd7a16974a76ee41317948f6ab4350daa88fc4`，已推送 `origin/main` 并读回远端确认。两份本地安装包、Pages 与 Toy 均使用该源码提交。
- `check:release`、普通版／Toy 版 TypeScript 与 Vite 构建、`check:toy`、Toy 版发布标识检查及 `toy_doctor.py` 内容预检通过；预检无 ERROR／WARN。
- Windows：`package:win:portable` 成功，PE 架构 x64、产品版本 1.8.5，内嵌前端入口资源通过项目脚本校验。
- Android：`package:android:arm64` 成功重新编译 1.8.5 原生库，之后因本机符号链接权限不足退出。校验新生成的 `libapp_lib.so` 版本、arm64 架构与内嵌资源后，使用既有 `package:android:arm64:reuse` 复制该库并完成 Gradle 装配与签名。
- APK：包名 `com.frostforge.pocpet`，`versionName=1.8.5`，`versionCode=10805`，仅 `arm64-v8a`；v2／v3 签名验证通过，证书与项目 arm64 签名基线一致。
- APK 签名证书 SHA-256：`E375653D29A6738BC45B1EF34B6B1B6BD86DDA66C53D751DDEE3683ACECCD285`。

## 本地产物

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `release/pocket1.8.5.exe` | 35,586,048 | `5C388826FD723940E4A15A76451D5FF6F9B1474AE7252DA9BF562ECBAE7254B8` |
| `release/pocket1.8.5.apk` | 38,196,803 | `617D95C53F46D4BEFDAEFB702C94E67E043CAD0B11B788C595B55562B6C7E4B9` |

## 部署结果

- GitHub Pages：[部署任务 34857722142](https://github.com/T-meow/PocPet/actions/runs/34857722142) 成功，使用 `source=main`、`version=1.8.5` 和完整源码提交 SHA。
- [Pages 正式站点](https://t-meow.github.io/PocPet/) 的 `build-info.json` 已确认 1.8.5／standard／`b4cd7a16974a76ee41317948f6ab4350daa88fc4`；入口 HTML、JS、CSS 均返回 200，CSS 与本地构建一致。
- Toy 已先生成预览并按用户本次明确部署授权提交，CLI 返回 `auditing` 后，`mylist` 确认状态为 `published`。
- [Toy 正式站点](https://www.bilibili.com/toy/pocpet/index.html) 实际加载版本 `23949807352832-v14700`；内容页的 `build-info.json` 确认 1.8.5／bilibili／同一源码提交，线上入口 JS 的 SHA-256 与上传目录一致。
- [本次 Toy 预览](https://www.bilibili.com/toy/preview/preview_yB7l225D/index.html)。自动核验只检查构建和 HTTP 内容，界面画面及真机体验由用户人工验收。
- 打包和部署完成。本记录作为验收提交补充推送；期间新增的 Furo 美术记录及试稿清单保留给对应任务，不纳入此次版本发布提交。
